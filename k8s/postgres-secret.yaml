apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: default
type: Opaque
data:
  # POSTGRES_PASSWORD: base64-encoded password
  # To generate: echo -n "your-secure-password" | base64
  # Example below uses "postgres" encoded (UPDATE THIS IN PRODUCTION!)
  POSTGRES_PASSWORD: cG9zdGdyZXM=
